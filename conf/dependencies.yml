# Application dependencies

require:
    - play 
    - play -> fbgraph 0.4:
             transitive: false

repositories:
  - provided:
      type:       local
      artifact:   "${application.path}/jars/rssutils-1.1.jar"
      contains:
        - com.sun.cnpi -> *

  - provided:
      type:       local
      artifact:   "${application.path}/jars/KeyPhraseExtraction.jar"
      contains:
        - pt.voiceinteraction -> *